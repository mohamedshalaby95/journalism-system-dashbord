export interface IPostAction {
  type: string;
  payload?: any;
}

export interface Ipost {
  // _id: string;
  title: string;
  description: string;
  // image: string;
  category: string;
  subCategory: string;
  // auther: string;
  // region: string;
  // status: string;
}
